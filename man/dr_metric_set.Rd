% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric-set.R
\name{dr_metric_set}
\alias{dr_metric_set}
\title{Define related metrics with shared defaults}
\usage{
dr_metric_set(
  product,
  ...,
  dimensions = character(),
  time_column = NULL,
  time_behavior = if (is.null(time_column)) "flow" else "stock",
  units = "",
  owner = "",
  version = "1.0.0",
  approved = FALSE,
  code_version = NULL,
  na_policy = "reject"
)
}
\arguments{
\item{product}{Input asset id shared by all metrics.}

\item{...}{Named tidy summary expressions, as in \code{\link[dplyr:summarise]{dplyr::summarise()}}.}

\item{dimensions, time_column, owner, version, approved, code_version, na_policy}{Shared arguments passed to \code{\link[=dr_metric]{dr_metric()}}.}

\item{time_behavior}{One shared \code{"stock"} or \code{"flow"} value, or a named
vector covering every metric. Defaults to stock with a time column.}

\item{units}{One shared unit, or a named vector. Omitted names have no unit.}
}
\value{
An ordinary named list of \code{metric} definitions.
}
\description{
Captures named summary expressions without evaluating them. Returns an
ordinary named list of \code{\link[=dr_metric]{dr_metric()}} definitions for \code{dr_measure(metrics = ...)}.
Each id combines the product id and expression name, separated by a dot.
Business approval is explicit; exploratory definitions need no code version.
\code{dimensions} lists permitted grouping and filtering columns; it does not
choose a report layout. Select the grouping with \code{dr_measure(by = "company")}
or request an overall total explicitly with \code{dr_measure(by = character())}.
}
\examples{
definitions <- dr_metric_set(
  "orders", total = sum(amount), count = dplyr::n(),
  dimensions = "company", units = c(total = "EUR")
)
}
