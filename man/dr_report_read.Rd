% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{dr_report_read}
\alias{dr_report_read}
\title{Read an immutable report and its saved results}
\usage{
dr_report_read(lake, id, values_only = FALSE)
}
\arguments{
\item{lake}{Connected lake, lake configuration or local lake folder.
Supplied connections remain open; internally opened connections always close.}

\item{id}{Report release ID.}

\item{values_only}{Return only the named result tables.}
}
\value{
A manifest list; with \code{values_only}, a named list of tibbles for
ordinary reports or a long tibble for batch reports.
}
\description{
Reads the saved manifest without recalculating any metric. It preserves the
original calculation times and works on read-only lakes. Values use the
JSON representation stored in the report; dates are ISO strings. With \code{values_only}, ordinary reports return
named result tibbles; batch reports return the long measurement table, with
Date selections restored in the \code{.period} list column.
}
\examples{
# After saving report.v1 with dr_report_release():
# dr_report_read(lake, "report.v1", values_only = TRUE)
}
