% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{dr_metric}
\alias{dr_metric}
\title{Define a metric for exploration or approved reporting}
\usage{
dr_metric(
  id,
  product,
  expr = NULL,
  compute = NULL,
  dimensions = character(),
  time_column = NULL,
  time_behavior = if (is.null(time_column)) "flow" else "stock",
  unit = "",
  owner = "",
  description = "",
  version = "1.0.0",
  approved = FALSE,
  na_policy = "reject",
  empty_policy = "error",
  code_version = NULL,
  input_columns = NULL
)
}
\arguments{
\item{id, version}{Identity and version.}

\item{product}{Input asset id.}

\item{expr}{Tidy evaluation summary expression, e.g. sum(reserve).}

\item{compute}{Alternative function(data, dimensions, params) for complex
metrics.}

\item{dimensions}{Permitted grouping columns.}

\item{time_column}{Column representing the business date.}

\item{time_behavior}{\code{stock} requires exactly one selected date; \code{flow} may
span dates. Defaults to stock when \code{time_column} is supplied, otherwise flow.}

\item{unit}{Optional unit.}

\item{owner, description}{Optional business metadata.}

\item{approved}{Whether business review is complete.}

\item{na_policy}{Policy description; must be consistent with the expression.}

\item{empty_policy}{Policy description.}

\item{code_version}{Version of metric code and dependencies. Required for
approved definitions; exploratory definitions may omit it.}

\item{input_columns}{Optional explicit input columns for dynamic expressions
or custom functions. Known expression columns are always checked as well.}
}
\value{
Metric specification. Reports execute it directly without an LLM.
}
\description{
Define a reusable calculation, such as a total or cancellation rate, for a
data product. Choose its grouping columns, unit and date behavior, then
calculate it with \code{\link[=dr_measure]{dr_measure()}}.
}
\examples{
metric <- dr_metric(
  "orders.total", "orders", expr = sum(amount), time_behavior = "flow",
  unit = "EUR", owner = "Analytics", description = "Total order value",
  approved = TRUE, code_version = "v1"
)
metric
}
